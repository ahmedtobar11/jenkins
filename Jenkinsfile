pipeline {
    agent any

    stages {
        stage('check') {
            steps {
                echo 'check is running'
            }
        }

      stage('build') {
            steps {
                echo 'building is running ...'
            }
        }

      stage('test') {
            steps {
                echo 'testing is running .....'
            }
        }
    }
}
