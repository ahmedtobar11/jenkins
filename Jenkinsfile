pipeline {
    agent any

    stages {

      stage(“build”) {
            steps {
                echo 'building is running ...'
            }
        }

      stage(“test”) {
            steps { 
                echo 'testing is running ...'
            }
        }

        stage(“deploy”) {
            steps {
                echo 'deploying is running ...'
            }
        }
    }
}
